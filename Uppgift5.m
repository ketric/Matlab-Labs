% a) 
%Hur anger man antal punkter man vill ha?
%y = linspace(x1,x2,n) genererar n punkter
%Hur många punkter får man som standard
%om man inte anger något antal?
%100

% b)
%x = fzero(fun,x0) tries to find a point x where fun(x) = 0.
%This solution is where fun(x) changes sign—fzero
%cannot find a root of a function such as x^2.

%x = fzero(fun,x0,options) uses options to modify the solution process.

%x = fzero(problem) solves a root-finding problem specified by problem.